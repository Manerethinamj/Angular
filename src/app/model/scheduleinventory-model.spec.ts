import { ScheduleinventoryModel } from './scheduleinventory-model';

describe('ScheduleinventoryModel', () => {
  it('should create an instance', () => {
    expect(new ScheduleinventoryModel()).toBeTruthy();
  });
});
